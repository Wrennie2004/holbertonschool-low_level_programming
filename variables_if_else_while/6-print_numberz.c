#include <stdio.h>


/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int i;
	i = 48;
        
	while (i <= 57)
        {
                putchar(i);
                i = i + 1;
        }
        putchar('\n');

    return (0);
}
